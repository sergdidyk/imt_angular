import { FormatResultPipe } from './format-result.pipe';

describe('FormatResultPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatResultPipe();
    expect(pipe).toBeTruthy();
  });
});
